name: list of all the s3 buckets

on:
  workflow_dispatch:
jobs:
  new-job:
    runs-on: ubuntu-latest

    steps:
      - name: git checkout
        uses: actions/checkout@v3

      - name: aws creditals
